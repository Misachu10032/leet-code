

## Follow-ups / gotchas

brute force: pop and insert k times pop last element is O(1). insert first is O(n). so O(k x n)

similar to linked list. to rotate an array 
reverse the whole thing. 
reverse to k-1 index
reverse k to end
This is O(3 x n) ish

